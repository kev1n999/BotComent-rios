export async function searchVideos(searchMethod: string) {
    if (searchMethod.trim().toLowerCase() === "hashtag") {
        
    } else if (searchMethod.trim().toLowerCase() === "search") {

    }
};